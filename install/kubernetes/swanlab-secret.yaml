apiVersion: v1
kind: Secret
metadata:
  name: swanlab-secret
  namespace: pipeline
  labels:
    app: swanlab
    component: api-key
type: Opaque
stringData:
  # 将 <YOUR_SWANLAB_API_KEY> 替换为 SwanLab Self-hosted 实例的 API Key
  # API Key 获取方式：浏览器打开 http://10.121.177.227:8000 → Settings → API Keys
  SWANLAB_API_KEY: "ys1VH6So4njMstkmqtTpV"
